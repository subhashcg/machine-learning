"""7. Grade

A student's grade: a `score` (0-100), a read-only `letter` derived from it, and
a `passed` boolean also derived.

Decide for each of the three whether it should be a plain attribute, a
@property, or a method — and write a comment justifying each choice. Then
implement it.

Make `score` reject values outside 0-100. Show that changing `score` updates
`letter` and `passed` with no extra work.
"""


class Grade:
    # TODO — comment your three design decisions before implementing
    pass


if __name__ == "__main__":
    # TODO: build one, print score / letter / passed

    # TODO: change the score, print again — nothing else should need updating

    # TODO: reject an out-of-range score
    pass
