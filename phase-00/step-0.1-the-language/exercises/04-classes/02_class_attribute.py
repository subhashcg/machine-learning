"""2. Basket and BasketOk

Basket puts `items = []` at class level. BasketOk builds it in __init__.

Show two instances of each, with output that makes the sharing obvious. Then
prove it with `is`, not just by eye.

In a comment, explain why self.items.append(x) reaches the class attribute while
self.items = [x] would not.
"""


class Basket:
    # TODO — the shared version
    pass


class BasketOk:
    # TODO — the per-instance version
    pass


if __name__ == "__main__":
    # TODO: two of each, add different things, print

    # TODO: prove it with `is`

    # TODO: comment — read falls through, write lands locally
    pass
